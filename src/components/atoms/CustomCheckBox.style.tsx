import styled from '@emotion/native';
import { rsHeight } from '../../utils/responsive-size';

export const Container = styled.View`
  gap: ${rsHeight * 20 + +'px'};
  padding-bottom: ${rsHeight * 16 + 'px'};
`;
